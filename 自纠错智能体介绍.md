# 自纠错解题智能体（SelfCorrect）

> 一个人独立完成的实战项目。
> 关键词：AI Agent、自我纠错、反思、工具反馈、评估、科研入门。
> 你要回答的问题：**自我纠错，到底什么时候有用、什么时候是帮倒忙？**

---

## 一、项目背景

大模型算错了、写错了代码，能不能自己发现、自己改对？直觉上好像可以——让它"再检查一遍"就行。

但你可能也遇到过：它本来是对的，你追一句"你确定吗"，它反思一通反而把答案改错了。

- 2023 年，很多论文（Self-Refine、Reflexion）说"反思"很神，能显著提升。
- 2024 年，DeepMind 却发论文说：**离开外部反馈，模型根本纠不对自己的推理，甚至越改越差**；很多"提升"其实偷偷用了标准答案来决定"何时停止修改"。

到底谁对？这个项目要做的不是"让模型多改几遍"，而是你**自己实现一条由弱到强的自我纠错方法链，并用实验回答**：

> 自我纠错在什么条件下真的有用、什么时候只是自我安慰？

核心是一个循环：

```text
解一道题
  -> 对自己的答案给出反馈（自己想 or 借工具验证）
  -> 根据反馈改写
  -> 失败就把教训记下来，下次带着重做
  -> 用实验判断：这一步到底让它变好了没有
```

---

## 二、你要做什么

你要独立实现一个项目：

```text
self-correct-agent
```

一个能解题、并会"自查自改"的 Agent + 一套能说明问题的评测。核心目标是：**让 Agent 从反馈中改进，并用数据证明这些"自我纠错"到底提升了多少、什么时候反而添乱。**

先挑一个**能明确判对错**的任务域（这样才好做实验）：

| 任务域 | 说明 |
|---|---|
| 数学题（GSM8K）| 小学应用题，答案是数字、好判分；**没有现成外部验证** |
| 编程题（HumanEval）| 有单元测试，跑一下就知道对错 = **天然的外部反馈** |
| 多跳问答（HotpotQA）| 需要多步推理，可选 |

先用其中一个跑通，行有余力再加一个——数学题（无外部验证）和代码题（有测试）正好形成对比。自拟任务须满足：①能明确判对错；②有一定难度、baseline 会错一部分；③最好能获得某种外部反馈。

---

## 三、项目核心问题

你的系统至少要回答下面 5 个问题：

1. **什么时候该自我纠错？** 简单题可能根本不需要，难题才有空间。
2. **反馈从哪来最有效？** 模型自己空想的反馈、外部工具（跑代码/单测/计算器）的反馈、还是标准答案？
3. **反思怎么写才有用？** 空话（"要更仔细"）和具体教训（"上次漏算了税"）差多少？
4. **迭代几轮合适？** 反思到第几轮开始没用、甚至变差？
5. **怎么证明它真有用？** 和不自我纠错的 baseline 相比，是更准，还是其实把对的改错了？——而且**判断"要不要采纳修改"时绝不能偷用标准答案**。

---

## 四、最小可交付版本

不要求复杂，但必须完整。

### 1. 任务与评测

- 一个数据集（如 GSM8K / HumanEval 的 100 题子集）。
- 一个**判分器**：从模型输出里抽出答案、判断对错（数学题注意数字后带单位/符号；代码题用单元测试）。

### 2. 由弱到强的 Agent 变体（接口统一，只改一处）

统一一个接口 `solve(question) -> (answer, trace)`，实现四个版本：

- **baseline**：直接解，不自查
- **self_refine**：解 → 自评 → 有 calculator 证据才改写
- **reflexion**：失败后写一条"教训"存进记忆，下次重试时带上
- **critic**：反馈不靠空想，而是**跑代码 / 单元测试 / 计算器**验证后再改

### 3. 一次求解的记录长什么样（便于分析）

```json
{
  "question_id": "gsm8k_017",
  "method": "self_refine",
  "attempts": [
    {"round": 1, "answer": "13", "correct": false, "feedback": "第二步乘错了", "feedback_source": "self"},
    {"round": 2, "answer": "12", "correct": true,  "feedback": "", "feedback_source": "self"}
  ],
  "final_correct": true
}
```

### 4. 运行入口（CLI 即可）

```bash
python main.py solve "小明有3个苹果，又买了2袋每袋4个……"
python main.py eval --method baseline      # method: baseline / self_refine / reflexion / critic
```

---

## 五、推荐技术路线

### 推荐目录结构

```text
self-correct-agent/
  main.py
  agents/
    base.py          # 直接解 / ReAct
    self_refine.py
    reflexion.py
    critic.py
  tools/
    code_runner.py   # 代码执行沙箱
    calculator.py
  data/
    dataset.jsonl
    datasets/
  eval/
    run_eval.py
    metrics.py
  results/
    evaluation_report.md
    when_correction_helps.svg
  logs/
    solve_trace.jsonl
    reflection_log.jsonl
  docs/
    paper_notes.md
    research_proposal.md
  README.md
  failure_review.md
```

### 推荐工具

| 模块 | 推荐 |
|---|---|
| 编程语言 / CLI | Python，argparse / Typer |
| 数据集 | GSM8K（数学）/ HumanEval（代码，有单测）|
| 工具反馈 | 代码执行沙箱（subprocess/exec 隔离）、计算器 |
| 反思记忆检索 | 简单可用 list；进阶用 embedding + 向量库 |
| LLM API | DeepSeek / OpenAI-compatible API（先用便宜模型）|

> 铁律：**固定随机种子和 temperature**，LLM 调用结果缓存，否则数字不可复现、实验白做。

---

## 六、6 周推进路线

### 第 1 周：Baseline 与判分

目标：跑出第一个能信的准确率。

任务：选任务域，取 100 题；写好判分器；实现 baseline（直接解 + 逐步想）；跑出准确率；记录几个失败案例。

本周产出：`data/dataset.jsonl`、`eval/run_eval.py`、baseline 数字、`failure_review.md`。

思考题：哪些错是"模型能力不够"，哪些是"其实能力够、只是没检查出来"？

### 第 2 周：Self-Refine（自我反馈—改写）

目标：让它给自己挑毛病、再改一版。

任务：实现 `self_refine.py`（生成 → 自评 → 改写），并保留两种模式：原始 r1 直接采纳模型自评，以及只有 calculator 证明算术不一致才改写的门控模式；在同一批题上对比 baseline；统计改对/改错。

本周产出：`agents/self_refine.py`、`logs/solve_trace.jsonl`、对比表。

思考题：Self-Refine 是"改对的题"多，还是"改错的题"多？真去数一数。

### 第 3 周：Reflexion（反思记忆）

目标：从"当场改"升级到"记仇下次改"。

任务：实现 `reflexion.py`（失败写反思、存记忆、重试带上）；评测改成"允许重试"（每题最多 3 次）；对比"有反思 vs 无反思"。

本周产出：`agents/reflexion.py`、`logs/reflection_log.jsonl`、成功率对比。

思考题：空话反思和具体教训，效果差多少？（真做个对比）

### 第 4 周：CRITIC（工具验证的反馈）

目标：把反馈从"空想"换成"外部工具"。

任务：实现代码执行 / 单元测试 / 计算器验证；做关键对照——**"自评反馈" vs "工具反馈"**，其它不变；看在哪类题上差最大。

本周产出：`tools/`、`agents/critic.py`、反馈来源对比表。

思考题：为什么代码题（有单测）比数学题更容易靠自我纠错改对？

### 第 5 周：关键实验（回答研究问题）

目标：正面回答"自我纠错何时有用"。

任务：设计实验——**有/无外部反馈 × 不同题型 × 迭代轮数**；特别测"无对错信号时多轮纠错会不会越改越差"；画一张核心图。

本周产出：`results/evaluation_report.md`、一张"什么时候有用"的核心图。

思考题：你判断"要不要采纳修改"时，有没有偷偷用标准答案？（那正是 DeepMind 批评的坑）

### 第 6 周：展示、论文阅读与科研提案

任务：3 分钟 demo；`docs/paper_notes.md`；`docs/research_proposal.md`（1 页）。

思考题：你的实验结果，支持还是反驳《LLMs Cannot Self-Correct Reasoning Yet》？

---

## 七、论文阅读路线

至少精读 3 篇，选读 2 篇。不要只写摘要，要说明论文如何影响你的实现。

### 必读

1. **ReAct: Synergizing Reasoning and Acting in Language Models**（Yao et al., 2022）https://arxiv.org/abs/2210.03629
2. **Self-Refine: Iterative Refinement with Self-Feedback**（Madaan et al., 2023）https://arxiv.org/abs/2303.17651
3. **Reflexion: Language Agents with Verbal Reinforcement Learning**（Shinn et al., 2023）https://arxiv.org/abs/2303.11366

### 关键 + 选读

4. **CRITIC: LLMs Can Self-Correct with Tool-Interactive Critiquing**（Gou et al., 2024）https://arxiv.org/abs/2305.11738
5. **Large Language Models Cannot Self-Correct Reasoning Yet**（Huang et al., DeepMind, 2024）—— 唱反调的对照标尺 https://arxiv.org/abs/2310.01798
6. **ExpeL: LLM Agents Are Experiential Learners**（Zhao et al., 2024）https://arxiv.org/abs/2308.10144
7. **Self-Consistency Improves Chain of Thought Reasoning**（Wang et al., 2022）https://arxiv.org/abs/2203.11171
8. **Chain-of-Verification Reduces Hallucination**（Dhuliawala et al., 2023）https://arxiv.org/abs/2309.11495

### 论文阅读模板（写进 `docs/paper_notes.md`）

```markdown
## Paper title
### 1. 它解决什么问题？
### 2. 它的自我纠错 / 反馈机制是什么？
### 3. 它如何评估？（有没有用到标准答案？）
### 4. 它有什么局限？
### 5. 对我的实现有什么启发？
```

---

## 八、参考开源项目

可以参考，但不允许只包装它们当作自己的项目。

- **Reflexion 官方仓库**（noahshinn/reflexion）—— episodic reflection 的实现思路
- **Self-Refine 官方仓库**（madaan/self-refine）—— 自评-改写循环
- **HumanEval / lm-evaluation-harness** —— 代码题评测、判分参考
- 其它方法（CRITIC 等）：在 GitHub 搜论文名找作者复现

建议先读 README，再挑 1-2 个模块深入。

---

## 九、科研延展方向（想继续深入可选一个）

- **方向 A · 收益递减曲线**：反思迭代 1/2/3/… 轮，准确率怎么变？什么时候由升转降？
- **方向 B · 反馈来源对比**：自评 vs 工具 vs 标准答案，三者提升差多少？（复现 Huang 的核心发现）
- **方向 C · 反思写法**：空话 / 具体教训 / 带"错误类型"标签，哪种最有效？
- **方向 D · 任务分型**：推理题 / 工具题 / 知识题，自我纠错的收益各是多少？
- **方向 E · 树搜索**：让它探索多条解题路径再择优（LATS，https://arxiv.org/abs/2310.04406），和纯反思比。

---

## 十、怎样算做得不错（给你自己对照，不是打分）

- **问题清晰**：任务域明确，判分可靠。
- **架构清楚**：几个方法变体接口统一，方便控制变量对比。
- **能复现**：README 能跑通，固定了种子/temperature。
- **机制完整**：自评、反思记忆、工具反馈都实现了。
- **有实验**：有 baseline、有对照、有一张"何时有用"的核心图。
- **能反思**：能解释它什么时候帮忙、什么时候帮倒忙——讲清失败比只展示成功更值钱。
- **会读论文**：能把论文思想映射到自己的实现。

## 十一、你最后会攒下这些（就是你的作品）

项目代码、`README.md`、`results/evaluation_report.md`、核心图、`logs/solve_trace.jsonl`、`logs/reflection_log.jsonl`、`docs/paper_notes.md`、`docs/research_proposal.md`（≤1 页）、`failure_review.md`。

## 十二、最后做个 3 分钟演示（给自己/同学讲）

30" 问题（自我纠错到底行不行）→ 45" 方法（自评/反思/工具反馈怎么做）→ 45" 实验（何时有用，看核心图）→ 30" 失败案例 → 30" 下一步。

---

一句话：这个项目不是让智能体"多改几遍"，而是搞清楚——**自我纠错什么时候真能变好、什么时候只是自我安慰**，并用数据说话。
